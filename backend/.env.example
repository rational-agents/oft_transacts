# =============================================================================
# OFT Transacts Backend Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# Never commit .env to version control!

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# SQLite is the default for local development
DATABASE_URL=sqlite:///backend/var/oft.sqlite3

# For production, consider PostgreSQL:
# DATABASE_URL=postgresql://username:password@localhost:5432/oft_transacts
# Example with cloud provider:
# DATABASE_URL=postgresql://user:pass@your-db-host.cloud.provider.com:5432/oft_db

# =============================================================================
# OIDC AUTHENTICATION (Required)
# =============================================================================
# OpenID Connect issuer URL - get this from your OIDC provider
# This is the base URL that provides the .well-known/openid-configuration endpoint
OIDC_ISSUER=https://your-tenant.okta.com/oauth2/default

# Examples for different providers:
# Okta: https://your-tenant.okta.com/oauth2/default
# Auth0: https://your-tenant.auth0.com
# Keycloak: https://your-keycloak.com/realms/your-realm
# Azure AD: https://login.microsoftonline.com/your-tenant-id/v2.0

# OIDC Audience (Optional)
# Only required if your OIDC provider enforces a specific 'aud' claim
# Leave empty if not required by your provider
OIDC_AUDIENCE=

# Examples:
# For Auth0 API: https://your-api-identifier
# For Okta: api://default or your custom audience
# For most providers: leave empty

# =============================================================================
# CORS CONFIGURATION
# =============================================================================
# Comma-separated list of allowed origins for CORS
# Include all URLs where your frontend will be hosted
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:4173

# Production example:
# ALLOWED_ORIGINS=https://your-app.com,https://www.your-app.com

# =============================================================================
# PAGINATION SETTINGS
# =============================================================================
# Number of transactions to return per page
# Adjust based on your performance requirements
TRANSACTS_PAGE_SIZE=10

# =============================================================================
# OPTIONAL: ADDITIONAL SETTINGS
# =============================================================================
# Uncomment and configure if needed

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO

# Enable debug mode (DO NOT use in production)
# DEBUG=false