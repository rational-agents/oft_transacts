# =============================================================================
# OFT Transacts Frontend Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# Never commit .env to version control!

# =============================================================================
# API CONFIGURATION
# =============================================================================
# Backend API base URL
# Development: http://localhost:8000
# Production: https://your-api-domain.com
VITE_API_BASE_URL=http://localhost:8000

# =============================================================================
# OIDC AUTHENTICATION (Required)
# =============================================================================
# OpenID Connect issuer URL - must match your OIDC provider
# This should match the OIDC_ISSUER in your backend configuration
VITE_OIDC_ISSUER=https://your-tenant.okta.com/oauth2/default

# Examples for different providers:
# Okta: https://your-tenant.okta.com/oauth2/default
# Auth0: https://your-tenant.auth0.com
# Keycloak: https://your-keycloak.com/realms/your-realm
# Azure AD: https://login.microsoftonline.com/your-tenant-id/v2.0

# OIDC Client ID - get this from your OIDC provider's SPA application
# This is different from the backend configuration
# Create a Single Page Application (SPA) in your OIDC provider
VITE_OIDC_CLIENT_ID=your_spa_client_id_here

# =============================================================================
# IMPORTANT NOTES
# =============================================================================
# 1. All VITE_ prefixed variables are exposed to the client browser
# 2. NEVER put secrets or API keys in frontend environment variables
# 3. The OIDC_CLIENT_ID is safe to expose (it's for a public SPA client)
# 4. Redirect URIs must be configured in your OIDC provider:
#    - Sign-in redirect: http://localhost:5173/signin-callback
#    - Sign-out redirect: http://localhost:5173/
#    - For production, add your production URLs

# =============================================================================
# OPTIONAL: DEVELOPMENT SETTINGS
# =============================================================================
# Uncomment if needed for debugging

# Enable detailed OIDC logs in browser console
# VITE_OIDC_DEBUG=true